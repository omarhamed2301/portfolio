// import { createContext, useState } from "react";

// // Create a custom React context for handling theme state

// export let Counter = createContext(0);

// export default function counterProvider(props) {
//   const [count, setCount] = useState(0);
//   function increment() {
//     setCount(count + 1);
//   }
//   function decrement() {
//     setCount(count - 1);
//   }

//   return (
//     <Counter.Provider value={{ count, increment, decrement }}>
//       {props.children}
//     </Counter.Provider>
//   );
// }
